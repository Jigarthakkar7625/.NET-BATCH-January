import { MyDemoDirective } from './my-demo.directive';

describe('MyDemoDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDemoDirective();
    expect(directive).toBeTruthy();
  });
});
